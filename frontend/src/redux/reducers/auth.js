const intialState = {
  isAuth: false,
  user: {},
};

function dispatcher(state = intialState, action) {
  switch (action.type) {
    default:
      return state;
      break;
  }
}

export default dispatcher;
