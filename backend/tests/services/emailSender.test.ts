// const { emailSender } = require("../../src/services");

// const MAX_TESTING_TIME_IN_MS = 20000;

// describe.skip("Email Sender", () => {
//   // the passing of this test is that it does not throw.
//   //ensure that the internet connection is stable and strong since the email
//   //sender has problem with slow internet connectivity.
//   it(
//     "should send email",
//     async () => {
//       const email = {
//         to: "samuelmayna@gmail.com",
//         subject: "Testing that emails can be sent",
//         html: `<p>Testing that an email can be sent from the application.</p>
//                        `,
//       };
//       await emailSender(email);
//     },
//     MAX_TESTING_TIME_IN_MS
//   );
// });
